# Dev-mode test configuration (local Windows, DirectX)
# Compiles hot-reload infrastructure and runs HotReloadTest.
# Run: haxe test-hx-multianim-dev.hxml && hl build/hl-test-dev.hl

test-common.hxml
-lib hldx
-D MULTIANIM_DEV
-hl build/hl-test-dev.hl
