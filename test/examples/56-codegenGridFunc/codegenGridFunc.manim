version: 0.5

#codegenGridFunc programmable() {
  grid: 80, 60

  repeatable($i, step(3, dx: 1)) {
    bitmap(generated(color($grid.width, $grid.height, #448844))): $i * $grid.width, $i * $grid.height
  }
}
