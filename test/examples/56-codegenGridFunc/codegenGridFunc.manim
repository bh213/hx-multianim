version: 0.3

#codegenGridFunc programmable() {
  grid: 80, 60

  repeatable($i, step(3, dx: 1)) {
    bitmap(generated(color(function(gridWidth), function(gridHeight), #448844))): $i * function(gridWidth), $i * function(gridHeight)
  }
}
